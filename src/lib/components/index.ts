import { GitHubIcon, GoogleIcon, Logo, XIcon } from '@/lib/components/asset'
import { PrimaryButton, SecondaryButton, WithIconButton } from '@/lib/components/button'
import { Input } from '@/lib/components/form'
import { Clock1, Clock2, Clock3, Clock4, Clock5, Clock6, Clock7, Clock8, Clock9, ClockAlert } from '@/lib/components/icon'

export const components = {
	// button
	PrimaryButton,
	SecondaryButton,
	WithIconButton,

	// form
	Input,

	// asset
	GitHubIcon,
	GoogleIcon,
	Logo,
	XIcon,

	// icon
	// * clock
	Clock1,
	Clock2,
	Clock3,
	Clock4,
	Clock5,
	Clock6,
	Clock7,
	Clock8,
	Clock9,
	ClockAlert,
}
