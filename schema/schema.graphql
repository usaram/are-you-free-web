# GraphQL schema example
#
# https://gqlgen.com/getting-started/

schema {
  query: Query
  mutation: Mutation
}

type Query
type Mutation

extend type Query {
  healthCheckForBackend: String!
}

interface BasePayload {
  isSuccess: Boolean!
}
